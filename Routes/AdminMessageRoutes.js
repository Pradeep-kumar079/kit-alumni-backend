// Routes/AdminMessageRoutes.js
const express = require("express");
// const router = express.Router();
// const verifyToken = require("../middleware/authMiddleware");
// const verifyAdmin = require("../middleware/VerifyAdmin");
// const upload = require("../multerconfig");
// const { sendMessage, getSentMessages } = require("../Controllers/adminMessageController");

// router.post("/send-message", verifyToken, verifyAdmin, upload.single("image"), sendMessage);
// router.get("/messages", verifyToken, verifyAdmin, getSentMessages);

// module.exports = router;
